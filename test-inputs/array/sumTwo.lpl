// inputs: 13
// output: 28
def unit test(int x) {
  int[] b;
  b = new int[2];
  b[0] = x; b[1] = x;
  inc(b);
  output sumTwo(b);
}

def unit inc(int[] a) {
  int i; i = 0;
  while (i < (a.length)) {
    a[i] = (a[i]) + 1;
    i = i + 1;
  }
}

def int sumTwo(int[] a) {
  return (a[0]) + (a[1]);
}
