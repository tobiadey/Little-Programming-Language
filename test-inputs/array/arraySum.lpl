// output: 4
def unit main() {
  int[] m;
  m = new int[3];
  //m[0]= 0;
  //m[1]= 0;
  //m[2]= 0;
  m[0] = 1 + (sum(m));
  m[2] = 1 + (sum(m));
  output 1 + (sum(m));
}

def int sum(int[] a) {
  int n;
  int i;
  n = 0;
  i = 0;
  while (i < (a.length))
  {
    n = n + (a[i]);
    i = i + 1;
  }
  return n;
}
